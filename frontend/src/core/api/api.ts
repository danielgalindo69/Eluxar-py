/**
 * @deprecated Este archivo es un puente de compatibilidad hacia la nueva
 * estructura modular en src/core/api/index.ts
 *
 * Para importar, actualiza tus rutas a:
 *   import { productsAPI } from '../../../core/api/api';
 * (sin cambios necesarios — este archivo re-exporta todo desde los módulos)
 */
export * from './index';
